import fs from 'node:fs';

const file = process.argv[2] || 'index.html';
const raw = fs.readFileSync(file, 'utf8');
const eol = raw.includes('\r\n') ? '\r\n' : '\n';
let html = raw.replace(/\r\n/g, '\n');
const original = html;
const buildDate = new Date().toISOString().replace(/\.\d{3}Z$/, 'Z');

function replaceOnce(search, replacement, label) {
  const count = html.split(search).length - 1;
  if (count !== 1) throw new Error(`${label}: expected exactly one match, found ${count}`);
  html = html.replace(search, replacement);
}

replaceOnce('<meta name="app-version" content="6.6.3" />', '<meta name="app-version" content="6.7.0" />', 'meta version');
replaceOnce("const APP_CONFIG = Object.freeze({\n  name: 'Classroom Seating Planner',\n  shortName: 'Seating Planner',\n  version: '6.6.3',", "const APP_CONFIG = Object.freeze({\n  name: 'Classroom Seating Planner',\n  shortName: 'Seating Planner',\n  version: '6.7.0',", 'APP_CONFIG version');
html = html.replace(/  buildDate: '2026-09-04T13:23:57Z',/, `  buildDate: '${buildDate}',`);

const releaseNeedle = `const RELEASE_HISTORY = [\n  {\n    version: '6.6.3',\n    date: 'September 4, 2026',\n    title: 'Hosted PWA completion and Drive Picker deployment readiness',\n    current: true,`;
const releaseReplacement = `const RELEASE_HISTORY = [\n  {\n    version: '6.7.0',\n    date: 'September 4, 2026',\n    title: 'Production hardening, public contracts, and collaboration history',\n    current: true,\n    changes: [\n      'Added deterministic source splitting and single-file rebuild validation, browser regression CI, release packaging, and public save/encryption JSON Schemas without changing data schema 13 or encryption envelope 3.',\n      'Added repository-level MIT licensing, security, privacy, and data-handling documentation plus deployment guidance for Google OAuth and hosted releases.',\n      'Fixed collaboration editing notices so presence metadata survives normalization and is actually included in saved planner documents.',\n      'Added a bounded collaboration activity ledger showing editing notices, detected remote revisions, merge outcomes, and recent shared-file activity.',\n      'Preserved the existing live seat guidance, fairness/history tools, scheduled plans, visual comparison, CSV/image/support exports, comments, template libraries, mobile workflow, and Analytics default-on behavior.'\n    ]\n  },\n  {\n    version: '6.6.3',\n    date: 'September 4, 2026',\n    title: 'Hosted PWA completion and Drive Picker deployment readiness',\n    current: false,`;
replaceOnce(releaseNeedle, releaseReplacement, 'release history');

const normalizeOld = `function normalizeCollaborationAccess(value = null) {\n  const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};\n  const policies = {};\n  const rawPolicies = source.policies && typeof source.policies === 'object' && !Array.isArray(source.policies) ? source.policies : {};\n  Object.entries(rawPolicies).forEach(([permissionId, policy]) => {\n    const normalized = normalizeCollaborationPolicy(policy, { permissionId });\n    if (normalized.permissionId) policies[normalized.permissionId] = normalized;\n  });\n  return { version: COLLABORATION_ACCESS_VERSION, updatedAt: String(source.updatedAt || ''), policies };\n}`;
const normalizeNew = `function normalizeCollaborationAccess(value = null) {\n  const source = value && typeof value === 'object' && !Array.isArray(value) ? value : {};\n  const policies = {};\n  const rawPolicies = source.policies && typeof source.policies === 'object' && !Array.isArray(source.policies) ? source.policies : {};\n  Object.entries(rawPolicies).forEach(([permissionId, policy]) => {\n    const normalized = normalizeCollaborationPolicy(policy, { permissionId });\n    if (normalized.permissionId) policies[normalized.permissionId] = normalized;\n  });\n  const rawPresence = source.presence && typeof source.presence === 'object' && !Array.isArray(source.presence) ? source.presence : {};\n  const presence = Object.keys(rawPresence).length ? {\n    deviceId: String(rawPresence.deviceId || '').slice(0, 160),\n    email: String(rawPresence.email || '').slice(0, 200),\n    displayName: String(rawPresence.displayName || '').slice(0, 120),\n    workflow: String(rawPresence.workflow || '').slice(0, 40),\n    activeClassId: String(rawPresence.activeClassId || '').slice(0, 160),\n    updatedAt: String(rawPresence.updatedAt || '').slice(0, 40)\n  } : null;\n  const changeLedger = (Array.isArray(source.changeLedger) ? source.changeLedger : []).slice(-80).map(entry => ({\n    id: String(entry?.id || uid('collab-log')).slice(0, 180),\n    type: String(entry?.type || 'activity').slice(0, 60),\n    summary: String(entry?.summary || '').slice(0, 500),\n    deviceId: String(entry?.deviceId || '').slice(0, 160),\n    email: String(entry?.email || '').slice(0, 200),\n    displayName: String(entry?.displayName || '').slice(0, 120),\n    workflow: String(entry?.workflow || '').slice(0, 40),\n    activeClassId: String(entry?.activeClassId || '').slice(0, 160),\n    createdAt: String(entry?.createdAt || '').slice(0, 40)\n  }));\n  return { version: COLLABORATION_ACCESS_VERSION, updatedAt: String(source.updatedAt || ''), policies, presence, changeLedger };\n}`;
replaceOnce(normalizeOld, normalizeNew, 'collaboration normalization');

const updatePresenceNeedle = `  function updatePresence() {\n    state.collaborationAccess = state.collaborationAccess && typeof state.collaborationAccess === 'object'\n      ? state.collaborationAccess\n      : { version: COLLABORATION_ACCESS_VERSION, updatedAt: '', policies: {} };\n    const user = uiState.googleDriveCurrentUser || {};\n    state.collaborationAccess.presence = {\n      deviceId: appDeviceId(),\n      email: String(user.email || '').slice(0, 200),\n      displayName: String(user.name || user.displayName || '').slice(0, 120),\n      workflow: document.body.dataset.workflow || 'setup',\n      activeClassId: state.activeClassId,\n      updatedAt: new Date().toISOString()\n    };\n    state.collaborationAccess.updatedAt = new Date().toISOString();\n  }`;
const updatePresenceReplacement = `  function appendLedger(type, summary) {\n    state.collaborationAccess = normalizeCollaborationAccess(state.collaborationAccess);\n    const user = uiState.googleDriveCurrentUser || {};\n    const entry = {\n      id: uid('collab-log'),\n      type: String(type || 'activity').slice(0, 60),\n      summary: String(summary || '').slice(0, 500),\n      deviceId: appDeviceId(),\n      email: String(user.email || '').slice(0, 200),\n      displayName: String(user.name || user.displayName || '').slice(0, 120),\n      workflow: document.body.dataset.workflow || 'setup',\n      activeClassId: state.activeClassId,\n      createdAt: new Date().toISOString()\n    };\n    state.collaborationAccess.changeLedger = [...(state.collaborationAccess.changeLedger || []), entry].slice(-80);\n    state.collaborationAccess.updatedAt = entry.createdAt;\n    renderLedger();\n    return entry;\n  }\n\n  function updatePresence({ recordActivity = true } = {}) {\n    state.collaborationAccess = normalizeCollaborationAccess(state.collaborationAccess);\n    const user = uiState.googleDriveCurrentUser || {};\n    state.collaborationAccess.presence = {\n      deviceId: appDeviceId(),\n      email: String(user.email || '').slice(0, 200),\n      displayName: String(user.name || user.displayName || '').slice(0, 120),\n      workflow: document.body.dataset.workflow || 'setup',\n      activeClassId: state.activeClassId,\n      updatedAt: new Date().toISOString()\n    };\n    state.collaborationAccess.updatedAt = new Date().toISOString();\n    if (recordActivity) appendLedger('editing-notice', 'Recorded an editing notice for the shared planner.');\n  }\n\n  function renderLedger() {\n    const target = el('driveCollaborationLedger');\n    if (!target) return;\n    const access = normalizeCollaborationAccess(state.collaborationAccess);\n    const entries = [...(access.changeLedger || [])].slice(-20).reverse();\n    if (!entries.length) {\n      target.innerHTML = '<p class="muted">No collaboration activity has been recorded yet.</p>';\n      return;\n    }\n    target.innerHTML = entries.map(entry => {\n      const who = entry.displayName || entry.email || (entry.deviceId ? 'Another device' : 'This planner');\n      const when = entry.createdAt ? new Date(entry.createdAt).toLocaleString() : 'Unknown time';\n      return \`<div class="planning-history-item"><strong>\${escapeHtml(entry.summary || entry.type)}</strong><span>\${escapeHtml(who)} · \${escapeHtml(when)}</span></div>\`;\n    }).join('');\n  }`;
replaceOnce(updatePresenceNeedle, updatePresenceReplacement, 'presence and ledger functions');

replaceOnce(`        await SharedDriveCollaborationV64.mergeRemoteDriveChanges({ reason: 'polling-merge' });`, `        await SharedDriveCollaborationV64.mergeRemoteDriveChanges({ reason: 'polling-merge' });\n        appendLedger('merge', 'Reviewed and merged remote Google Drive changes.');\n        scheduleLinkedAutoSave('collaboration-merge-ledger');`, 'merge ledger');
replaceOnce(`        if (status) status.textContent = 'Remote changes found. Review and merge them before saving.';\n        setLiveStatusMessage('Remote Google Drive changes are available for review and merge.');`, `        if (status) status.textContent = 'Remote changes found. Review and merge them before saving.';\n        appendLedger('remote-change', 'Detected a newer remote Google Drive revision.');\n        setLiveStatusMessage('Remote Google Drive changes are available for review and merge.');`, 'remote change ledger');
replaceOnce(`<button id="recordDrivePresenceBtn" class="secondary" type="button">Record my editing notice</button></section></div></div>`, `<button id="recordDrivePresenceBtn" class="secondary" type="button">Record my editing notice</button></section><section class="section"><h3>Recent collaboration activity</h3><div id="driveCollaborationLedger" class="planning-history-list" aria-live="polite"><p class="muted">No collaboration activity has been recorded yet.</p></div></section></div></div>`, 'ledger modal section');
replaceOnce(`    if (el('drivePollingStatus')) el('drivePollingStatus').textContent = settings.enabled\n      ? \`Checks run every \${settings.minutes} minutes.\${last ? \` Last checked \${new Date(last).toLocaleString()}.\` : ''}\`\n      : 'Checks are disabled.';\n    DialogManager.synchronize();`, `    if (el('drivePollingStatus')) el('drivePollingStatus').textContent = settings.enabled\n      ? \`Checks run every \${settings.minutes} minutes.\${last ? \` Last checked \${new Date(last).toLocaleString()}.\` : ''}\`\n      : 'Checks are disabled.';\n    renderLedger();\n    DialogManager.synchronize();`, 'render ledger on open');
replaceOnce(`  return Object.freeze({ install, afterReady, open, checkNow, updatePresence });`, `  return Object.freeze({ install, afterReady, open, checkNow, updatePresence, appendLedger });`, 'DrivePolling API');

if (html === original) throw new Error('Patch made no changes.');
fs.writeFileSync(file, eol === '\n' ? html : html.replace(/\n/g, eol), 'utf8');
console.log(`Applied V6.7 runtime patch to ${file}.`);
