{
  "name": "classroom-seating-planner",
  "version": "7.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate:monolith-to-src": "node tools/split-monolith.mjs",
    "build": "node tools/build-single-file.mjs",
    "check": "node tools/validate-release.mjs",
    "test": "npm run check",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": {
    "node": ">=24"
  }
}
